box: alpine

build:
  steps:
    - script:
        name: install expect
        code: |
            apk update
            apk add expect
    - berquist/add-ssh-agent-key:
        key: FLEXNLP_MODELS_PRIVATE

publish:
  steps:
    - internal/publish-step:
        owner: berquist
